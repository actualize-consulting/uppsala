<?xml version="1.0" encoding="UTF-8"?>
<!-- Base type carrying a list-typed attribute (list of int), in its own
     namespace. Imported by both list-ext.xsd and list-composite.xsd. -->
<schema targetNamespace="urn:list-base"
        xmlns="http://www.w3.org/2001/XMLSchema"
        xmlns:b="urn:list-base" elementFormDefault="unqualified">
  <element name="e" type="b:Base"/>
  <complexType name="Base">
    <attribute name="nums" type="b:intList"/>
  </complexType>
  <simpleType name="intList"><list itemType="int"/></simpleType>
</schema>
