<?xml version="1.0" encoding="UTF-8"?>
<!-- Resolvable imported schema: declares a global element in its own namespace.
     The composite entry schema reaches this element only through xs:import. -->
<schema targetNamespace="urn:inner"
        xmlns="http://www.w3.org/2001/XMLSchema"
        xmlns:i="urn:inner"
        elementFormDefault="qualified">
  <element name="Thing" type="i:ThingType"/>
  <complexType name="ThingType">
    <attribute name="id" type="string"/>
  </complexType>
</schema>
